import styled from "styled-components";

const BlogAssetContainer = styled.div`
    width: 75%;
    margin: 1rem auto !important;

    @media (max-width: 900px) {
        width: 100%;
    }
`;

export default BlogAssetContainer;
